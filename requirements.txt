Problem:

I would like to build a system that syncs local folders with selected google drive folders.  The program should have ability to synchronize files and folders, in either direction. 


Specification:

The user logs into the system using google account via google oauth. The program must then show users drive account and shared drives that the user has access to.


The program must allow me to browse both local folders and google drive folders (both shared and users own folders). The it must provide a way for me to synchronize any folder or file in either direction. It must indicate the newer version where applicable. I should also be able to apply a filter to select certain folders or file types or filenames to select and synchronize them. 

The synchronization must show progress bar of how many files need to be synced, and how the sync is progressing. 

This program must have a very intuitive UI, that is user friendly and easy to use. It must make sensible choices where applicable. It must keep track of when the last sync was done, so I know how long it has been since the sync. If this state requires a small db such as sqllite it is fine or even if file based approach is also fine. I will always initiate this call from my desktop where the app runs. I should be able to run the entire app by doing docker compose up.

When the program is launched for the first time, it must guide me to setup required settings for google oauth, and persist those settings in db or some secure fashion.

It must have activity tracker that shows which files and folders were synced from where to where, along with timestamp, and user information.
If folder is selected for synchronization, it must do nested folder, unless I provide a specific filter to select only certain folders or files.

Show file or folder meta information about sizes, bytes transferred etc. It will be great, if we can do incremental syncup. 

I would like to also have a scheduler, that periodically synchronizes the selected files or folders.

Once application is built, it must have no dependency on requiring LLMs for any kind of processing. 

Think deeply about the problem, come up with a plan. Ask me for any clarifying question.

Help me build the skill for this problem, and keep all required context in canvas.md for this project. 